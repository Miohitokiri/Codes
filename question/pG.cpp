#include<bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio ( false );
	cin.tie ( 0 );

	string str;
	int sum;
	while ( cin >> str ){
		if ( str == "*" )
			break;

		if ( 'a' <= str[0] && str[0] <= 'z' ){
			sum = 0;
			cout << 
		}
	}
}