#include<bits/stdc++.h>
using namespace std;
int main(){
	ios::sync_with_stdio ( false );
	cin.tie ( 0 );
	int t, data[6][5];

	cin >> t;
	for ( int times = 0 ; times < t ; times++ ){
		// get data
		for ( int i = 0 ; i < 6 ; i++ )
			for ( int j = 0 ; j < 5 ; j++ )
				cin >> data[i][j];

		
	}
}