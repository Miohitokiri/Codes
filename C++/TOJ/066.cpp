#include<bits/stdc++.h>
using namespace std;
int main(){
	ios::sync_with_stdio ( false );
	cin.tie ( 0 );

	string in;
	while ( getline ( cin, in ) ){
		for ( int i = 0 ; i < in.size() ; i++ ){
			if ( in[i] == ' ' )
				continue;
		}
	}
}